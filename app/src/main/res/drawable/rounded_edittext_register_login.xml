<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Макет фигуры поля ввода. Для  отрисовки полей ввода данных регистрации и авторизации.
Структура:
<shape> — фигура;
<solid/> — сплошной цвет фигуры;
<stroke/> — контур фигуры;
<corners/> — закругленные углы фигуры;
<shape/>
    -->
    <solid android:color="@android:color/white" />
    <stroke
        android:width="1dp"
        android:color="@android:color/holo_blue_dark" />
    <corners android:radius="25dp" />

</shape>